# Atlas Knowledge Base frontend

Vue 3 + TypeScript + Vite SPA shell (TASK-002). Talks only to Atlas `/api/v1`
through the Vite dev proxy. Do not store provider or model credentials in
browser storage.

```sh
npm install
npm test
npm run build
npm run dev
```

Route placeholders: `/chat` (default), `/kbs`, `/settings`.

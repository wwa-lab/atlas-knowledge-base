/**
 * Session/BFF trust boundary: opaque Atlas session cookie, CSRF, and SSO
 * callback handling. Provider and model credentials must never leave this
 * boundary toward the browser.
 */
package com.atlas.knowledgebase.session;
